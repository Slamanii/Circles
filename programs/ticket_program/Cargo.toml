[package]
name = "ticket_program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = "0.31.1"
mpl-bubblegum = "1.3.0"
spl-noop = { version = "0.2.0", features = ["no-entrypoint"] }

[features]
idl-build = ["anchor-lang/idl-build"]